import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
   
    textHeader: {
        fontSize: 30,
        color: '#B99CE8',
        fontWeight: "bold",
       marginTop: 8
    
    }
})

export default styles